/**
 * The public API a Sacred Gold mod is written against.
 *
 * <p>Parameters here are non-null unless a parameter says otherwise, and every
 * reference a method hands back carries {@code @Nonnull} or {@code @Nullable}
 * so an IDE can say which is which before the mod runs.
 */
@ParametersAreNonnullByDefault
package dev.ancaria.coderpack.api;

import javax.annotation.ParametersAreNonnullByDefault;
